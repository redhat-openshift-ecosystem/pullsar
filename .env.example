# Quay API Tokens for apps/worker script
QUAY_API_TOKENS_JSON='{"org1":"token1","org2":"token2"}'

# Database Credentials
DB_NAME="pullsar_db"
DB_USER="user"
DB_PASSWORD="password"
DB_HOST="localhost"
DB_PORT=5432

# Client certificate files (optional)
# used for Pyxis API mutual TLS authentication
# without this, defaults to Kerberos authentication
# CLIENT_CERT_PATH="tls.crt"
# CLIENT_KEY_PATH="tls.key"
